# --- Python ---
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/
*.egg-info/
*.egg
dist/
build/
.pytest_cache/
.coverage
.coverage.*
htmlcov/
.tox/
.mypy_cache/
.ruff_cache/

# --- Node / TypeScript ---
node_modules/
# `packages/framework/dist/` IS committed — consumers install via `github:` ref
# (no npm publish), so the compiled output must be in the repo. The generic
# `dist/` rule above (a Python convention) otherwise excludes it.
!packages/framework/dist/
!packages/framework/dist/**
*.tsbuildinfo
.npm
.eslintcache

# --- IDE ---
.idea/
.vscode/
*.swp
*.swo
*~

# --- OS ---
.DS_Store
Thumbs.db

# --- CDK (just in case) ---
cdk.out/
.cdk.staging/

# --- Local secrets ---
.env
.env.local
*.local

